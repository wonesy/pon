package main

import (
    "container/list"
    "fmt"
)

// holds an instance of a game
type gameInstance struct {
    gameId int
    createdBy string
    isProtected bool
    gameHub hub
}

func createGameInstance() *gameInstance {
    newGame := new(gameInstance)
    
    (*newGame).gameId = assignGameId()
        
}

func getNewGameId() int {
    for i := range(currentGames) {
        
    }
}
